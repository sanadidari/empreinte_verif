✅ 2. Schémas d’architecture (ASCII + description)

(Version image prête à générer si tu veux un PNG)

📘 Architecture Globale
                          ┌──────────────────────┐
                          │      Utilisateur      │
                          └───────────┬───────────┘
                                      │
                                      ▼
                         ┌────────────────────────┐
                         │   Application Flutter   │
                         │  (Web + Android + iOS)  │
                         └───────────┬────────────┘
                                     │
                ┌────────────────────┼──────────────────────┐
                │                    │                      │
                ▼                    ▼                      ▼
      ┌──────────────────┐   ┌───────────────────┐   ┌──────────────────┐
      │ UI (Widgets)     │   │ HTTP Client       │   │ Service Worker   │
      │ Scanner, pages   │   │ Appel API /scan   │   │ Web caching      │
      └─────────┬────────┘   └─────────┬─────────┘   └─────────┬────────┘
                │                      │                        │
                ▼                      ▼                        ▼
      ┌──────────────────┐   ┌───────────────────┐   ┌──────────────────┐
      │ Build Flutter    │   │ Backend (à venir) │   │ CDN Vercel       │
      │ web / mobile     │   │ analyse empreinte │   │ assets + js      │
      └──────────────────┘   └───────────────────┘   └──────────────────┘

📗 Architecture Web (Vercel)
          Browser (Chrome / Edge)
                    │
           requests URL
                    │
                    ▼
            ┌──────────────┐
            │   Vercel     │
            │  Routing     │
            └──────┬───────┘
                   │
      ┌────────────┼──────────────────────────┐
      │            │                          │
      ▼            ▼                          ▼
 index.html   main.dart.js         flutter_bootstrap.js
 manifest     assets/icons         canvaskit/

📙 Architecture Code Flutter
lib/
 ├─ main.dart        → entry point, MaterialApp
 ├─ ui/
 │   └─ home.dart    → page principale Scan
 ├─ services/
 │   └─ api.dart     → appels HTTP (futur)
 ├─ widgets/
 │   └─ fingerprint_button.dart