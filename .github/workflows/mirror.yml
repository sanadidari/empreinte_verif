name: Mirror to Public Repo

on:
  push:
    branches: [ "main" ]

jobs:
  mirror:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout private repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.MIRROR_DEPLOY_KEY }}
          known_hosts: |
            github.com ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICW8WcN6iYG3PaY5E9O+V1YxDCEV4VpWwfvX2gCtYdZ

      - name: Add mirror remote
        run: |
          git remote add mirror git@github.com:sanadidari/empreinte_verif_mirror.git

      - name: Push to mirror
        run: |
          git push --force mirror main
